# =====================================================================
# Copia este archivo como  .env  y rellena la clave de servicio.
# NUNCA subas el .env a git — ya está en .gitignore.
# =====================================================================

# --- Supabase: proyecto "motor-causal" (us-east-1) -------------------
SUPABASE_URL=https://wobflrgzgcqjqwndmjal.supabase.co

# Clave PÚBLICA. Solo puede LEER (política RLS "lectura publica").
# Es la única que va en la app Dash. Si se filtra, no pasa nada grave.
SUPABASE_ANON_KEY=sb_publishable_OnOudaHino0Jxdp0_PkM_w_NNQZFevK

# Clave de ADMINISTRADOR. Salta RLS: puede borrar la base entera.
# Solo la usan los scripts de ingesta, nunca el frontend.
# Cópiala de: Dashboard > Project Settings > API Keys > service_role
SUPABASE_SERVICE_KEY=pega_aqui_tu_service_role_key

# --- LLM (fase 5, todavía no hace falta) -----------------------------
# Recuerda H-01: el modelo corre en lote nocturno, jamás en la ruta del usuario.
GROQ_API_KEY=
GEMINI_API_KEY=
